name: Read file manually

on:
 workflow_dispatch: 

jobs:
 read-file:
  runs-on: ubuntu-latest

  steps:
   - name: Check repository
     uses: actions/checkout@v4

    - name: Read app.txt
      run: cat app.txt
      
      
     
